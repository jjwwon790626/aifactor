
# Quick‑start demo files

* `predictions_sample.csv` – 30 trading‑day demo prediction file.
* `backtest_result_sample.png` – cumulative return curve based on the sample CSV.
* `shap_summary_sample.png` – placeholder feature‑importance graphic.

These artefacts are **placeholders** only. Replace them with the real outputs
once the full data pipeline and model training have run successfully.
